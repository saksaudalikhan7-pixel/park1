# Production Environment Variables for Azure App Service
# IMPORTANT: These MUST be configured in Azure Portal → App Service → Configuration → Application Settings

# Backend API URL (CRITICAL - Must include /api/v1)
# Configure in Azure as: NEXT_PUBLIC_API_URL
NEXT_PUBLIC_API_URL=https://ninjainflablepark-gbhwbbdna5hjgvf9.centralindia-01.azurewebsites.net/api/v1

# Azure Blob Storage URL
NEXT_PUBLIC_AZURE_STORAGE_URL=https://ninjaparkimages.blob.core.windows.net

# NOTE: This file is for reference only. 
# Azure App Service requires these to be set in the portal, not from this file.
# The build process will use these values if set in GitHub Actions environment.
